---
"git-env-vault": minor
---

Add monorepo DX improvements: `gitignore check/fix`, `refresh`, `diff`, `pull --confirm` with safe key-only diffs and selective updates, plus bug fixes for `grant`, `pull --no-write`, Windows `.sops.yaml` regexes, and correct CLI version output.
